import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.sleepyshibasnore',
  appName: 'sleepy-shiba-snore',
  webDir: 'dist'
};

export default config;
